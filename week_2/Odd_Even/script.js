
    let number = prompt('Check number type');

    if (number => 0 && (number%2) == 0) {
        console.log('The number is even')
    }   
    else if (number => 0 && (number%2) > 0) {
        console.log('The number is Odd');

    };
